
#include "Exceptions.hpp"
#include "StreamBase.hpp"

#include <string>

namespace xPlat {

    // A stream for calculating/checking a CRC32 digest
    class CRC32Stream : public StreamBase
    {
    public:

    };
}